# Docker Compose configuration. Copy to .env and replace these values.
POSTGRES_PASSWORD=local-demo-password-change-me
JWT_SECRET=replace-with-random-secret-at-least-32-characters
SEED_PASSWORD=DemoPassword123!
